{
  "name": "@watchingeye/agent-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LangGraph-based deterministic Super Agent DAG with zod guardrails. Orchestration only — no autonomous routing.",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@langchain/langgraph": "^0.2.0",
    "fastify": "^5.10.0",
    "jpeg-js": "^0.4.4",
    "onnxruntime-node": "^1.27.0",
    "tesseract.js": "^5.1.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
