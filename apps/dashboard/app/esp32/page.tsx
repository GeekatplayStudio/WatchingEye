import { Esp32FlasherPanel } from "@/components/esp32-flasher";

export const metadata = {
  title: "ESP32 Hardware Flasher & Telemetry | WatchingEye",
};

export default function Esp32Page() {
  return <Esp32FlasherPanel />;
}
