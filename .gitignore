/target
node_modules/
dist/
*.log
.env
.DS_Store
coverage/
models/

.next/
.runtime/

# TypeScript incremental build cache
*.tsbuildinfo

# Production build output (kept separate from the dev server's .next)
.next-prod/

# Regenerated by every next build/dev; its contents follow whichever
# distDir ran last, so tracking it is pure churn.
next-env.d.ts
